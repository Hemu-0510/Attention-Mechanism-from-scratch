This project implements the Attention Mechanism from scratch using Python and NumPy. It demonstrates how input embeddings are transformed into Query (Q), Key (K), and Value (V) matrices, computes scaled dot-product attention, generates attention weights using the Softmax function, and produces the final attention output. This project provides a simple and practical understanding of the core concept behind Transformer models.
